# ══════════════════════════════════════════════════════════════════════════════
#  NEXUS FRONTEND — Configuration
#  Copiez ce fichier en .env et adaptez les valeurs.
#  Les variables PUBLIC_* sont exposées côté client (navigateur).
# ══════════════════════════════════════════════════════════════════════════════

# URL de l'API backend Nexus (sans slash final)
# En développement local : http://localhost:3000/api/v1
# En production avec reverse proxy sur le même domaine : /api/v1
# En production sur domaine séparé : https://api.mondomaine.fr/api/v1
PUBLIC_API_URL=/api/v1

# ── WebRTC TURN server (optionnel) ────────────────────────────────────────────
# Requis quand les deux pairs sont derrière des NAT ou que le routeur ne
# supporte pas le hairpinning. Sans TURN, la connexion reste en "checking".
# Installer coturn sur un VPS : https://github.com/coturn/coturn
# Ou test rapide (gratuit, limité) : turn:openrelay.metered.ca:80
# PUBLIC_TURN_URL=turn:monserveur.com:3478
# PUBLIC_TURN_USERNAME=nexus
# PUBLIC_TURN_CREDENTIAL=motdepasse

# ── Nexus Signet (authentificateur PWA, optionnel) ───────────────────────────
# URL de l'instance Nexus Signet hébergée — laisser vide si non utilisé
# PUBLIC_SIGNET_URL=https://signet.nexusnode.app
PUBLIC_SIGNET_URL=
