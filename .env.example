# ══════════════════════════════════════════════════════════════════════════════
#  NEXUS — Docker Compose variables
#  Copy this file to .env (at the project root, next to docker-compose.yml).
#  This file is ONLY used by docker-compose.yml — not by the app itself.
# ══════════════════════════════════════════════════════════════════════════════

# PostgreSQL password — shared between the 'postgres' and 'api' services
# Generate a strong password: openssl rand -base64 32
DB_PASSWORD=changeme_in_production
